# Task Contract

Use for non-trivial implementation, debugging, refactoring, migration, or architecture work. Infer fields from source when safe; ask only when a missing decision changes product intent, public contracts, data, or security.

```markdown
Goal:
Current evidence:
In scope:
Out of scope:
Acceptance criteria:
Risk level and approval gates:
Verification commands:
```

Keep the contract short and preserve the user's original goal throughout implementation and reporting.

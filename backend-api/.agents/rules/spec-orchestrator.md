# Spec Orchestrator

Use only for implementation plans/handoffs.

Define goal, use case, route/API, aggregate boundary, validation, authorization, transaction/idempotency, persistence/schema impact, failure modes, tests, rollout gate, and explicit assumptions. Read the Commerce roadmap and current status. Require approval for public/schema/security/dependency changes. Produce executable batches with verification and no unresolved implementer decisions.
